version: 1

checkers:
  - id: rdap
    type: rdap

notifiers:
  - id: tg-ops
    type: telegram
    settings:
      bot_token: secret
      chat_id: "123"

domains:
  - name: example.com
    checker: rdap
    schedule: "every six hours"  # not a 5-field cron
    channels: [tg-ops]
