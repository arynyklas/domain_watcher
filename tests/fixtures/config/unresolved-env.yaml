version: 1

checkers:
  - id: rdap
    type: rdap

notifiers:
  - id: tg-ops
    type: telegram
    settings:
      bot_token: ${REQUIRED_BUT_MISSING_TOKEN}
      chat_id: "123"

domains:
  - name: example.com
    checker: rdap
    schedule: "0 */6 * * *"
    channels: [tg-ops]
